# jest-config-mono-package-clone

Clones [`jest.config.js`](./jest.config.js) to the dependent project/package.

## How to use

Run the cli, `yarn js-common-jest-config-mono-package-clone`. Now you have a `js.config.js` file.
