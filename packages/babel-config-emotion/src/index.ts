import babelConfig from './babel.config';

export {
  SourceType,
  EnvPlugins,
  Env,
  OurEnv,
  presets,
  envPlugins,
  env,
  plugins
} from './babel.config.internals';

export default babelConfig;
